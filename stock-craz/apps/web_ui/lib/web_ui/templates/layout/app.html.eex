<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="StockCraz">

    <title>StockCraz</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <%= if @user_signed_in? do %>
              <li><%= link "Stocks", to: stock_path(@conn, :index), class: "btn btn-outline-primary" %></li>
              <li><%= link "Portfolios", to: portfolio_path(@conn, :index), class: "btn btn-outline-primary" %></li>
              <li><%= link "Sign Out", to: session_path(@conn, :signout), class: "btn btn-outline-primary" %></li>
            <% else %>
              <li><%= link "Sign In", to: session_path(@conn, :new), class: "btn btn-outline-primary" %></li>
            <% end %>
          </ul>
        </nav>
        <span class="logo"></span>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
  </body>
</html>
